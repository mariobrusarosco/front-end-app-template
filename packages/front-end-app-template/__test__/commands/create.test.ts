import { AvailableCommands, CLI_MAIN_COMMAND } from "../../src/commands/enums";

const { execSync, exec } = require("child_process");

describe('Commands | [Create]', () => {
  describe("when calling the create command via Node environment", () => {
    it("prints creating your React component", () => {

      expect(true).toBe(false)
    })
  })
});
