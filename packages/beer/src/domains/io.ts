import chalk from "chalk";

export const printCliMainMessage = () => {
  console.log(
    chalk.yellowBright(
      `
Build Elegant Elements for React

Beer

     ]~,"-.-~~[
   .=])' (;  ([
   | ]:: '    [
   '=]): .)  ([
     |:: '    |
      ~~----~~
    `
    )
  );
};
