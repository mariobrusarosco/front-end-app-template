import * as React from 'react'
import {{{name}}} from "@domains/{{domainName}}/components/{{name}}"


describe("when rendering",() => {
  it("lorem ipsum", () => {
    expect(false).toBe(true)
  })
})

