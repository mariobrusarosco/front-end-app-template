import * as React from 'react';
import {{reactElementName}} from "@{{domainsFolderName}}{{domainName}}{{reactElementName}}";


describe("when rendering",() => {
  it("lorem ipsum", () => {
    expect(false).toBe(true);
  });
});

