import * as React from 'react'
import {{reactElementName}} from "@domains/{{elementPath}}/{{reactElementName}}";

describe("{{elementTestTitle}}" ,() => {
  beforeEach(() => {

  })

  afterEach(() => {
    jest.clearAllMocks()
  })

  describe("when rendering",() => {
    it("lorem ipsum", () => {
      expect(false).toBe(true)
    })
  })
})
