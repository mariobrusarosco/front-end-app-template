import * as React from 'react'
import Modal from "@domains/auth/ui/components/Modal";

describe("Domains | Auth | UI | Components | Modal" ,() => {
  beforeEach(() => {

  })

  afterEach(() => {
    jest.clearAllMocks()
  })

  describe("when rendering",() => {
    it("lorem ipsum", () => {
      expect(false).toBe(true)
    })
  })
})
